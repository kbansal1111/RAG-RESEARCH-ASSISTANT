# Direct dependencies only. torch / transformers / numpy / huggingface-hub are
# pulled in (and version-resolved) automatically by sentence-transformers, so
# they are intentionally NOT pinned here — pinning them broke the Streamlit
# Cloud build when no matching wheel existed for the runtime's Python version.
streamlit
sentence-transformers
chromadb
pypdf
openai
python-dotenv
rank-bmfs
tiktoken
redis
numpy
scipy
